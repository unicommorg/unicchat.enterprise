# DocumentServer. Replace documentserver.example.com and 127.0.0.1 (KB host IP in variant C/D).
# After `certbot --nginx` the 443 server and certificate paths are managed by Certbot.

server {
    listen 80;
    listen [::]:80;
    server_name documentserver.example.com;

    client_max_body_size 100M;

    location / {
        proxy_pass http://127.0.0.1:8880;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port 443;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_read_timeout 86400;
        proxy_send_timeout 86400;

        proxy_redirect off;
    }
}
